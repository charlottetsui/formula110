"""SAC training code for the Formula 110 controller (SAC track only).

See ``docs/rl_design.md`` for the design this package implements and
``CLAUDE.md`` for the session workflow that keeps this code and its docs in
sync.
"""
